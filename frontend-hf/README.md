# HuggingFace Frontend — Multilingual AI Orchestrator

This folder contains the Gradio-based frontend used for:
- Chat UI (EN/JP bilingual)
- Optional CSV upload
- Connecting to n8n backend webhook

To run locally:

pip install -r requirements.txt
python app.py
